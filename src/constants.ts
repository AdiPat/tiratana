type Path = string;

const IGNORE_DIRECTORIES = [".git", "node_modules", "dist", "build"];
const IGNORE_EXTENSIONS = [".report.txt", ".json", ".yaml"];

export { Path, IGNORE_DIRECTORIES, IGNORE_EXTENSIONS };
